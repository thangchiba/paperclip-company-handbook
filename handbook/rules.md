# Quy tắc công ty — Hoang LLC

**Phiên bản: v0.0 (khung chờ soạn thảo — CHƯA có hiệu lực)**

`rules.md` v0.1 sẽ được CEO soạn theo chỉ thị Board v0.1 mục 6 và gửi Board duyệt qua approval card. Trong lúc chờ, các quyết định đã có hiệu lực nằm trong `decisions/` (D-0001 → D-0007).

Các mục tối thiểu của v0.1:

1. Definition of Done
2. Protocol không đoán (needs-decision)
3. Approval matrix
4. Secret và dữ liệu khách
5. Giao tiếp (khung báo cáo ①②③)
6. Chi phí (heartbeat, budget tháng theo agent)
7. Mức tự chủ theo Project (`auto` / `notify` / `approve`)

Mọi thay đổi file này phải qua approval của Board (theo D-0002).
